1.  basic use of DOM manipulation to create a snake game.
2.  the columns and rows are not static but dynamic(they change according to the screen size and device). It is done using the concept of Math methods namely Random and Floor.
3.  used event listener to recognize input keys
4.  used a render function which handles food spawn, snake movement, wall collision and food collision
5.  also incorporated start game and restart game modals
